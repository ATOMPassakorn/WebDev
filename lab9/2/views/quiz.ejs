<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Questions</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
</head>

<body>
    <div class="container">
        <h1>Questions</h1>
        <form>
            <% data.forEach((item)=>{ %>
                <h3><%= item.QID %>. <%= item.Stem %></h3>
                <label class="radio-inline"><input type="radio" name="<%= item.QID %>"><%= item.Alt_A %></label><br>
                <label class="radio-inline"><input type="radio" name="<%= item.QID %>"><%= item.Alt_B %></label><br>
                <label class="radio-inline"><input type="radio" name="<%= item.QID %>"><%= item.Alt_C %></label><br>
                <label class="radio-inline"><input type="radio" name="<%= item.QID %>"><%= item.Alt_D %></label><br>
            <% }); %>
        </form>
    </div>
</body>

</html>